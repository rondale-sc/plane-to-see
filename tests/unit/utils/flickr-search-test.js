import flickrSearch from 'sw-planes/utils/flickr-search';
import { module, test } from 'qunit';

module('Unit | Utility | flickr search');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flickrSearch();
  assert.ok(result);
});
